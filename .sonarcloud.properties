# SonarCloud Automatic Analysis configuration.
# ci/source-build/config.sh is ~950 lines of vendored build shell for the
# from-source GDAL stack, deliberately kept minimal-diff against its
# upstream recipe; restyling it to satisfy shell rules would destroy that
# property. Our own scripts (build-gdal-stack.sh etc.) remain fully
# analyzed.
sonar.exclusions=ci/source-build/config.sh
